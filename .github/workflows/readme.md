Step 1 — Checkout     →  git clone your repo on server
Step 2 — Java         →  android needs java to build
Step 3 — Flutter      →  install flutter on server
Step 4 — Version      →  just a sanity check
Step 5 — pub get      →  download packages
Step 6 — Analyze      →  check for errors
Step 7 — Test         →  run tests
Step 8 — Build        →  create the apk
Step 9 — Upload       →  save apk for download